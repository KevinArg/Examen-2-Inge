  @model List<Laboratorio5.Models.ExpendingMachineModel>;


<div>
    <table class="table" >
        <thead>
            <tr>
                <th>Nombre</th>
                <th>Precio</th>
                <th>Cantidad</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>@item.nombre</td>
                    <td>@item.precio</td>
                    <td>@item.cantidad</td>
                </tr>
            }
        </tbody>
    </table>
</div>